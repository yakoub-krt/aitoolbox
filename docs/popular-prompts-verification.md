# Popular Prompts Verification

- The production-style empty state is visible on the homepage before any real copies are recorded, and it links readers to `/prompts`.
- A reversible end-to-end verification temporarily applied three real non-zero copy counts, confirmed ranking order `13 > 7 > 2`, direct copying from the populated card, and desktop and mobile rendering. The original zero counts were then immediately restored and verified.
- The mobile homepage was also captured with the real empty state to confirm the section remains readable before visitor copy data exists.
